# INSIZE ← shopmilltools enrichment evidence

Regenerate with:

```bash
PYTHONPATH=. python3 scripts/enrich_insize_from_shopmill.py --reuse-crawl --dry-run
```

Large crawl JSONL / site_export.jsonl are local (gitignored `/data/` except force-added reports).
