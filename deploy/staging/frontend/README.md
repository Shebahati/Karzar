# Frontend images for staging are built by:
#   deploy/staging/scripts/deploy-frontend.sh
#
# Dockerfiles:
#   Dockerfile.storefront — context = Storefront/
#   Dockerfile.admin — context = admin-panel/
#
# See ../STAGING_DEPLOY.md section 5.
