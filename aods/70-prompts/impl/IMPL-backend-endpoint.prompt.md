---
id: IMPL-backend-endpoint
version: 0.1.0
archetype: IMPL
role: ROLE-BE-IMPL
capability_class: CODE-GEN
reasoning_depth: R2
decision_ceiling: D2
lifecycle_state: Draft
parameters:
  - NODE_ID
  - SPEC_PATH
  - SPEC_SECTION
  - ENDPOINT_METHOD
  - ENDPOINT_PATH
  - ALLOWED_PATHS
context_tiers:
  T1: ["{{SPEC_PATH}}"]
  T2: [docs/ARCHITECTURE.md, app/api/endpoints/, app/services/, app/schemas/]
  T3: [docs/API_CONTRACT.md]
forbidden_context:
  - frontend/AI_CONTEXT.md
  - frontend/BACKEND_NON_COMPLIANCE.md
  - frontend/BACKEND_HANDOFF.md
  - docs/GO_LIVE_EXECUTION_PLAN.md
  - docs/audits/v1/
  - docs/audits/v2/SCORECARD-AFTER-REMEDIATION.md
gates: [lint, types, test, allowlist, citation]
produces: [CODE-DIFF, TASK-RECORD]
supersedes: null
---

## 1. AUTO MODE PROTOCOL — READ FIRST, OBEY EXACTLY

You are executing a single AODS node in Cursor Auto Mode. You have no memory of prior
sessions and there will be no follow-up conversation. This file is your entire brief.

HARD PROHIBITIONS (violating any one makes your output non-compliant and it will be discarded):
1. Do NOT modify, create, or delete any file outside ALLOWED SCOPE below.
2. Do NOT read any file listed under FORBIDDEN CONTEXT.
3. Do NOT refactor, reformat, rename, or "clean up" anything not required by the TASK.
4. Do NOT add, remove, or upgrade any dependency. That is a D3 decision — HALT instead.
5. Do NOT run git push, git merge, git rebase, git reset --hard, or any deploy command.
6. Do NOT invent a requirement. Every requirement you implement must be citable as path:line.
7. Do NOT claim a command passed without pasting its actual output.
8. Do NOT continue past a STOPPING CONDITION. Halting is a successful outcome.
9. Do NOT change a public contract (API response, DB schema, URL) unless the TASK says so.
10. Do NOT attempt a third strategy. Two attempts, then HALT.

REQUIRED PHASE ORDER: READ → RESTATE → PLAN → ACT → VERIFY → RECORD.
You MUST emit the RESTATE and PLAN blocks before your first edit.

IF ANYTHING IS UNCLEAR: halt using the HALT FORMAT. Do not guess. Do not pick the most
likely interpretation. An unclear specification is a defect in the specification, and
reporting it is the highest-value thing you can do in this run.

If any `{{PLACEHOLDER}}` below is still literally present, HALT immediately with trigger E1.

## 2. PURPOSE

Implement `{{ENDPOINT_METHOD}} {{ENDPOINT_PATH}}` exactly as specified in `{{SPEC_PATH}}` §{{SPEC_SECTION}},
changing nothing else.

## 3. ALLOWED SCOPE

You may create or modify ONLY these paths:

```
{{ALLOWED_PATHS}}
```

Typically this is one endpoint module, one service module, and one schema module. If the work
appears to need a fourth file, that is a signal the node is mis-scoped — HALT (E4).

## 4. FORBIDDEN SCOPE

| Path | Reason |
|------|--------|
| `alembic/**` | Schema changes are a separate node requiring `HC-08`; a migration hidden inside a feature PR is unreviewable |
| `docs/**` | Documentation is a separate `DOC` node (atomicity) |
| `openapi/v1.json` | Regenerated by the paired `DOC` node, not by hand |
| `frontend/**` | Different surface, different role |
| `scripts/**` | Ingestion boundary; governed by ADR-012 |
| `.github/**` | CI changes are `OPS` work and can affect deploys |
| `requirements*.txt`, `pyproject.toml` | Dependency changes are D3 → HALT |
| `tests/**` | Tests are a separate `TEST` node |

Deleting any file is forbidden in every archetype. Supersede instead.

## 5. FORBIDDEN CONTEXT

Do NOT read these files. They are stale, superseded, or known to contain false claims,
and reading them causes hallucinated requirements:

| Path | Why |
|------|-----|
| `frontend/AI_CONTEXT.md` | ~1,000 lines of confirmed-false architecture claims (CR-015) |
| `frontend/BACKEND_NON_COMPLIANCE.md` | Obsolete gap ledger; presents resolved issues as open (CR-015) |
| `frontend/BACKEND_HANDOFF.md` | Same class of staleness (CR-015) |
| `docs/GO_LIVE_EXECUTION_PLAN.md` | Pre-launch plan contradicted by the live site (CR-014) |
| `docs/audits/v1/**` | Superseded by the v2 audit generation |
| `docs/audits/v2/SCORECARD-AFTER-REMEDIATION.md` | Self-certified 9.0 against a 5.7 audit (CR-006) |

## 6. INPUTS

Read these files, in this order, to this depth. Read NOTHING else.

| # | Tier | Path | Depth | Why you need it |
|---|------|------|-------|-----------------|
| 1 | T3 | `docs/API_CONTRACT.md` | §list-response + §error-envelope | The response envelope and error shape all endpoints share |
| 2 | T2 | `app/api/endpoints/<neighbour>.py` | FULL | The router/dependency/response-model conventions to imitate |
| 3 | T2 | `app/services/<neighbour>_service.py` | FULL | Service-layer conventions |
| 4 | T2 | `docs/ARCHITECTURE.md` | §"Transaction ownership (BE-01)" | The transaction rule you must not violate |
| 5 | T1 | `{{SPEC_PATH}}` | §{{SPEC_SECTION}} FULL | **Defines correctness.** The exact payload, status codes, and error cases |

If any T1 input is missing, unreadable, or does not contain the section named: HALT (E1).

## 7. ARCHITECTURE RULES

| # | Rule | Citation |
|---|------|----------|
| 1 | HTTP endpoint handlers own `await db.commit()` / `rollback()`. Services and CRUD **flush only**. | `docs/ARCHITECTURE.md:57` |
| 2 | Layering is API Route → Service → CRUD → Model. An endpoint must not query the ORM directly; a service must not import FastAPI request objects. | `docs/ARCHITECTURE.md` §layered architecture |
| 3 | List responses and the error envelope follow the documented shapes; do not invent a new envelope. | `docs/API_CONTRACT.md` |
| 4 | Paths are versioned under `/api/v1`; changing a path shape is a breaking change requiring an ADR. | `docs/API_CHANGELOG.md` §versioning policy |
| 5 | Every response field must appear in the spec. A field the spec does not mention must not be returned. | `{{SPEC_PATH}}` §{{SPEC_SECTION}} |

If the TASK appears to require violating one of these, HALT (E2) and report the conflict.

Rule 1 is violated by existing code in some services (`CR-021`). **Existing violations are not
precedent.** Do not imitate them; if a neighbour service commits, note it in your task record
under "Discovered but NOT fixed" and follow the documented rule in your own code.

## 8. FILE MODIFICATION RULES

- **Create:** only files named in ALLOWED SCOPE.
- **Modify:** only the functions required. Do not touch unrelated handlers in the same module.
- **Never delete** any file.
- **Never move or rename** any file.
- **Never reformat** untouched lines. Do not run a formatter across a whole file — the diff must
  show only your change, or the reviewer cannot see what you did.
- **Never add a dependency.** If the spec seems to require one, HALT (E3).
- **Never change an existing response field's name, type, or nullability.** That is a breaking
  contract change (D3) → HALT.
- Type-annotate everything you add; `mypy app` must stay clean.

## 9. TASK

Implement `{{ENDPOINT_METHOD}} {{ENDPOINT_PATH}}`:

1. Add the Pydantic response schema to the schemas module, with exactly the fields listed in
   `{{SPEC_PATH}}` §{{SPEC_SECTION}} — no extras, no omissions.
2. Add the service function containing the query and business logic. It must **flush only**, never commit.
3. Add the endpoint handler: dependency injection matching neighbours, `response_model` set,
   and the transaction boundary owned here per BE-01.
4. Implement every error case the spec names, with the spec's status codes and the documented error envelope.
5. If the spec is silent on a detail that does not affect the contract (e.g. internal sort order of an
   internal list), choose the simplest option consistent with neighbours and **log it under §Decisions**
   in the task record. That is your D2 ceiling.
6. If the spec is silent on something contract-visible (a field's presence, a status code, pagination
   behaviour), HALT (E3). Do not choose.

## 10. EXPECTED OUTPUTS

1. Code changes confined to `{{ALLOWED_PATHS}}`.
2. A task record at `aods/reports/tasks/{{NODE_ID}}.md` per
   `aods/40-artifacts/ARTIFACT-ARCHITECTURE.md` §4, including the `Assumptions` section
   (expected: empty) and `Discovered but NOT fixed`.
3. A response following OUTPUT FORMAT (§14).

You do **not** produce: tests, documentation, the OpenAPI snapshot, or a PMO update. Those are
separate nodes. Name them as follow-ups in the task record.

## 11. VALIDATION CHECKLIST

| # | Command | Expected |
|---|---------|----------|
| 1 | `ruff check app` | Exit 0, no findings |
| 2 | `mypy app` | Exit 0, no new errors |
| 3 | `pytest -q -m "not slow"` | All pass; no new failures vs the base commit |
| 4 | `python3 -c "from app.main import app; print(len(app.routes))"` | App imports cleanly; route count increased by the expected number |
| 5 | `git diff --name-only` | Every path is inside `{{ALLOWED_PATHS}}` |
| 6 | `git diff -- app/services app/crud \| grep -n "commit()"` | **No output.** Any hit is a BE-01 violation |
| 7 | `python3 aods/tools/aods_validate.py --gate allowlist --node {{NODE_ID}}` | Exit 0 |

If a command cannot be run in this environment, say so explicitly in the task record and
mark the criterion UNVERIFIED. Never assert an unrun command passed.

Item 6 expecting **no output** is deliberate: it is the one architecture rule this repository
already violates in existing code, so it needs a mechanical check rather than a reminder.

## 12. STOPPING CONDITIONS

Halt immediately, using the HALT FORMAT, if any of these is true:

1. `{{SPEC_PATH}}` does not exist, or does not contain §{{SPEC_SECTION}}. (E1)
2. `{{SPEC_PATH}}` does not resolve on the current merge base — for example it exists only on an
   unmerged branch. (E2 — this is the `CR-001` failure mode; never proceed.)
3. The spec's status is not `Accepted`. (E2 — architecture-first: implementation before acceptance is forbidden.)
4. The spec is silent on a contract-visible detail. (E3)
5. The work requires a database migration. (E4 — separate node, requires `HC-08`.)
6. The work requires a new dependency. (E3)
7. The work requires editing a path outside ALLOWED SCOPE. (E4)
8. An existing response contract would have to change. (E3)
9. Two attempts have failed. (E5)
10. Any `{{PLACEHOLDER}}` remains unfilled. (E1)

## 13. FAILURE HANDLING

- Attempt 1 fails → read the actual error; try ONE alternative strategy.
- Attempt 2 fails → HALT with both attempts and their exact errors.
- A third strategy is forbidden.
- If a test fails, fix your code. **Never** edit a test to make it pass — tests are outside ALLOWED SCOPE,
  so attempting it is also a scope violation.
- If `mypy` complains about existing untyped code you did not write, do not fix it here. Record it as
  discovered debt.
- Never add `# type: ignore` or `# noqa` to silence a checker. If a checker is genuinely wrong, HALT and say so.

## 14. OUTPUT FORMAT

Respond in exactly this structure:

```
## RESTATE
Governing authority:
  - {{SPEC_PATH}} §{{SPEC_SECTION}} (<path>:<line>) — <the required payload/behaviour in your own words>
    [resolves on merge base: YES/NO]  [status: Accepted? YES/NO]
Constraints I must not violate:
  - BE-01 endpoints own commit/rollback (docs/ARCHITECTURE.md:57)
  - Layering API → Service → CRUD → Model (docs/ARCHITECTURE.md:<line>)
  - Error envelope shape (docs/API_CONTRACT.md:<line>)
Not specified, and I will therefore NOT invent:
  - <gap> → <halting? yes/no, and why>

## PLAN
Files I will change (must be a subset of ALLOWED SCOPE):
  1. <path> — <why>
Files I will NOT change although related:
  1. tests/** — separate TEST node <NODE-ID>
  2. openapi/v1.json — separate DOC node <NODE-ID>
  3. docs/API_CONTRACT.md — separate DOC node <NODE-ID>

## ACT
<the edits>

## VERIFY
<verbatim command output for every item in §11>

## RECORD
Task record written to: aods/reports/tasks/{{NODE_ID}}.md

STATUS: COMPLETE
```

On halt, replace everything from `## ACT` onward with:

```
STATUS: HALTED
NODE: {{NODE_ID}}
TRIGGER: <E1|E2|E3|E4|E5>
BLOCKER:
  1. <what is unknown or conflicting>
     EVIDENCE: <path>:<line> says X; <path>:<line> says Y
     OPTIONS:
       A) <option> — consequence
       B) <option> — consequence
     RECOMMENDATION: <A|B> because <reason grounded in a cited authority>
     DECISION REQUIRED FROM: <role / HC-nn>
WORK COMPLETED BEFORE HALT: <files touched, or "none">
STATE OF REPOSITORY: <clean | uncommitted changes listed>
RESUME INSTRUCTIONS: <what a future stateless agent needs to continue>
```
