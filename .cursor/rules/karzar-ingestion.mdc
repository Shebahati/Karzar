---
description: Catalog scripts are hazardous — local Category A unless fail-closed B
globs: scripts/**
alwaysApply: false
---

Read `docs/architecture/data-ingestion-policy.md` and `scripts/ingestion_boundary.py`. Default `KARZAR_API_BASE=http://127.0.0.1:8000/api/v1`. Production host requires both `KARZAR_ALLOW_PRODUCTION_WRITE=1` and `KARZAR_INGESTION_CATEGORY=B`.
